version: '3.8'

services:
  tracker-app:
    build: .
    container_name: programming-tracker
    ports:
      - "3739:3739"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3739
      - DB_PATH=/app/data/tracker.db
    restart: unless-stopped

version: '3.8'

services:
  tracker-app:
    build: .
    container_name: programming-tracker
    ports:
      - "3739:3739"
    volumes:
      - ./tracker.db:/app/tracker.db
    environment:
      - PORT=3000
    restart: unless-stopped
